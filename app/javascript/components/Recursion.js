import React from "react";

function Recursion() {
  return (
    <>
      <h2 className="mt-5">Recursion - Recursive Algorithms</h2>
      <p>
        A recursive algorithm is an algorithm which calls itself with "smaller
        (or simpler)" input values, and which obtains the result for the current
        input by applying simple operations to the returned value for the
        smaller (or simpler) input.
      </p>
    </>
  );
}

export default Recursion;
