import React, {Component} from "react";

class DivideConquer extends Component {
  render() {
    return (
      <>
        <h2 className="mt-5">Divide and Conquer Algorithm</h2>
        <p>
          This pattern uses objects or sets to collect values/frequencies of
          values.
        </p>
        <p>
          This can often avoid the need for nested loops or O(n^2)O(n 2 )
          operations with arrays/strings.
        </p>
      </>
    );
  }
}

export default DivideConquer;
